import styled from 'styled-components';

export const Container = styled.nav`
  height: 100%;

  ul {
    display: flex;
    gap: 10px;
    align-items: center;
  }

  a {
    position: relative;
    height: 100%;
    padding: 10px 0;
  }
`;
